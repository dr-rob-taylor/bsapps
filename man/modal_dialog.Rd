% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modal.R
\name{modal_dialog}
\alias{modal_dialog}
\title{Create a Bootstrap modal dialog}
\usage{
modal_dialog(
  id = NULL,
  header = NULL,
  body = NULL,
  footer = modal_footer(modal_button(label = "Close")),
  fade = TRUE,
  centered = FALSE,
  scrollable = FALSE,
  easy_close = TRUE,
  esc_close = TRUE
)
}
\arguments{
\item{id}{A unique HTML id for the modal element, used to target it with a
toggle button.}

\item{header}{A [modal_header()] tag, or `NULL` to omit.}

\item{body}{A [modal_body()] tag, or `NULL` to omit.}

\item{footer}{A [modal_footer()] tag. Defaults to a footer with a close button.}

\item{fade}{If `TRUE`, the modal animates in and out with a fade transition.}

\item{centered}{If `TRUE`, the modal is vertically centered on the screen.}

\item{scrollable}{If `TRUE`, the modal body becomes independently scrollable
when its content overflows.}

\item{easy_close}{If `TRUE`, clicking the backdrop dismisses the modal.}

\item{esc_close}{If `TRUE`, pressing Escape dismisses the modal.}
}
\value{
An HTML tag that can be added to a UI definition.
}
\description{
Creates the outer container for a Bootstrap modal dialog. Use alongside
[modal_header()], [modal_body()], and [modal_footer()] to compose the
full modal, and [toggle_button()] with `toggle_type = "modal"` to trigger it.
}
\examples{
# No run
}
\seealso{
[modal_header()], [modal_body()], [modal_footer()], [modal_button()],
  [toggle_button()]
}
