% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/offcanvas.R
\name{offcanvas_dialog}
\alias{offcanvas_dialog}
\title{Create a Bootstrap offcanvas dialog}
\usage{
offcanvas_dialog(
  ...,
  id,
  header = NULL,
  body = NULL,
  footer = NULL,
  position = c("left", "right", "top", "bottom"),
  body_scroll = FALSE,
  backdrop = TRUE
)
}
\arguments{
\item{...}{Currently unused. Reserved for future use.}

\item{id}{A unique HTML id for the offcanvas element, used to target it with
a toggle button.}

\item{header}{An [offcanvas_header()] tag, or `NULL` to omit.}

\item{body}{An [offcanvas_body()] tag, or `NULL` to omit.}

\item{footer}{Optional footer content, or `NULL` to omit.}

\item{position}{Which edge of the viewport the panel slides in from. One of
`"left"`, `"right"`, `"top"`, or `"bottom"`.}

\item{body_scroll}{If `TRUE`, the page body remains scrollable while the
offcanvas panel is open.}

\item{backdrop}{If `TRUE`, a backdrop is shown behind the panel and clicking
it closes the panel.}
}
\value{
An HTML tag that can be added to a UI definition.
}
\description{
Creates the outer container for a Bootstrap offcanvas panel. Use alongside
[offcanvas_header()] and [offcanvas_body()] to compose the full panel, and
[toggle_button()] with `toggle_type = "offcanvas"` to trigger it.
}
\examples{
# No run
}
\seealso{
[offcanvas_header()], [offcanvas_body()], [toggle_button()]
}
